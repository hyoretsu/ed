#include <stdio.h>

int main(void) {
 float *numbers;

 printf("Digite o vetor de inteiros: ");
 scanf("%f, %f", &numbers[0], &numbers[1]);

 printf("%f", numbers[0]);
 printf("%f", numbers[1]);

 return 0;
}
